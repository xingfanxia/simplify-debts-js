# Settle brand master

`settle-app-icon-gpt.png` is the production raster master generated with the
built-in GPT Image tool on August 13, 2026. It is intentionally kept in the
repository so every web and native derivative can be reproduced with
`scripts/generate-brand-assets.sh`.

Final generation prompt:

> Refine the supplied Settle logo while preserving two opposing rounded
> arrow-ribbons forming a compact S. Make the geometry optically balanced and
> legible at 24px. Use acid lime, coral, and deep forest; no text, currency
> symbols, extra elements, mockup, border, or 3D treatment. Center the mark with
> generous safe space for iOS and Android icon masks.

The resulting image keeps a subtle color lift from GPT Image. The mark remains
clear at 24px and uses a strong silhouette suitable for app launchers.
